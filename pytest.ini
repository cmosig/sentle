[pytest]
testpaths = tests
markers =
    e2e: end-to-end tests that download real data from Planetary Computer
        (network + slow). Skipped unless the SENTLE_RUN_E2E environment
        variable is set.
