# Minimum supported dependency versions -- the *floors* declared in setup.py's
# install_requires, pinned exactly. CI installs with these constraints (see the
# "minimum" leg in .github/workflows/tests.yml) so we actually verify the lower
# bounds we promise users, not just the latest releases (requirements-ci.txt).
#
# If a job using this file goes red, the corresponding floor in setup.py is too
# low (sentle uses an API newer than that version) -- raise the floor here and
# in setup.py to match.
affine==2.4.0
filelock==3.30.0
geopandas==1.1.4
joblib==1.5.3
numpy==2.5.1
pandas==3.0.3
planetary-computer==1.0.0
pystac==1.15.1
pystac-client==0.9.0
rasterio==1.5.0
scipy==1.18.0
sen2nbar==2024.6.0
shapely==2.1.2
torch==2.13.0
tqdm==4.68.4
xarray==2026.7.0
zarr==3.2.1
