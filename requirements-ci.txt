# Pinned dependency versions used ONLY by CI (see .github/workflows/tests.yml).
#
# The shipped package keeps its open ``>=`` ranges in setup.py -- users always
# get the latest compatible releases. This file exists so Dependabot has
# concrete versions to bump: when an upstream release lands, Dependabot opens a
# PR editing this file, which runs the test suite against the new version. A
# breaking release (e.g. setuptools >=81 dropping pkg_resources, or zarr >=3.2
# changing StoreLike) therefore shows up as a red PR check instead of a user
# report. Keep this list in sync with setup.py's install_requires.
affine==3.0.0
filelock==3.32.2
geopandas==1.1.4
joblib==1.5.3
numpy==2.5.2
pandas==3.0.5
planetary-computer==1.0.0
pystac==1.15.2
pystac-client==0.9.0
rasterio==1.5.1
scipy==1.18.0
sen2nbar==2024.6.0
setuptools==84.0.0
shapely==2.1.2
torch==2.13.0
tqdm==4.70.0
xarray==2026.7.0
zarr==3.3.0
